# ~/.circleci/config.yml
version: 2
jobs:
  test:
    docker:
      - image: cimg/<language>:<version TAG>
        auth:
          username: udaybhosale3421
          password: Github@3421  # context / project UI env-var reference
    parallelism: 4
